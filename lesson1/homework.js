// 1. Write a recursive function which calculates n factorial. Draw a table which will have two columns: N and factorial of N

// 2. Write a recursive function which asks name for user using prompt and keeps asking until user enters proper name. Proper name: at least 4 characters
